import { NavHistory } from "@/types/mutual-funds";
import React from "react";
import { View } from "react-native";

const Returns = ({ navData }: { navData: NavHistory[] }) => {
  return <View></View>;
};

export default Returns;
